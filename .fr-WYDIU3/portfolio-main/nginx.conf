server {
    listen 80;
    root /usr/share/nginx/html;

    location / {
        try_files $uri $uri.html $uri/index.html =404;
    }

    location ~* \.(js|css|woff2|ttf|png|jpg|svg|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}

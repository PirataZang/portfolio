services:
  portfolio:
    build:
      context: .
      target: dev
      args:
        VITE_BASE_URL: ${VITE_BASE_URL:-}
    ports:
      - "4322:4322"
    volumes:
      - .:/app
      - /app/node_modules # keep the image's node_modules, not the host's